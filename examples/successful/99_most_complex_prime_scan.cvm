let limit = input;
let n = 2;

while (n < limit) {
  let isPrime = true;
  let d = 2;

  while (d < n) {
    let q = n / d;
    let back = q * d;

    if (back == n) {
      isPrime = false;
    }

    d = d + 1;
  }

  if (isPrime == true) {
    print n;
  }

  n = n + 1;
}
